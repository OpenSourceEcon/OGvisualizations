# OGvisualizations
Visualizations from large-scale overlapping generations model

### How to run:
* Clone repository
* Run `cd surf3Dtime/`
* Run `pip install -r requirements.txt`
* Run `python 3Dvis.py`
